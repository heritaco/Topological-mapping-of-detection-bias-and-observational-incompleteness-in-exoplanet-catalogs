Las limitaciones principales incluyen sesgo observacional por \texttt{discoverymethod}, variables derivadas no independientes, imputacion que no equivale a observacion, dependencia de parametros Mapper, y el hecho de que un \(\beta_1\) alto no implica automaticamente estructura fisica. La distancia \texttt{metric\_zscore\_l2} es una distancia entre vectores de metricas estandarizadas, no una distancia topologica estricta. \texttt{observed} \(\neq\) \texttt{physically\_derived} \(\neq\) \texttt{imputed}. % observed != physically_derived != imputed
